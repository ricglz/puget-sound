services:
  - type: web
    name: puget-sound
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python process_gtfs.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DEMO_MODE
        value: "false"
